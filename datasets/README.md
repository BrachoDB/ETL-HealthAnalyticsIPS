# Datasets

Estructura:
- `raw/`: datos crudos (originales) subidos o entregados.
- `processed/`: salidas intermedias o limpiezas.
- `exports/`: artefactos exportados (CSV/Excel/PDF) derivados.

Los scripts del ETL y/o endpoints pueden usar estos directorios como base para almacenamiento opcional.

